---
import testimonails from "../../src/data/testimoial.json";
import CardTestimonialL from "./widgets/Card-Testimonial-L.astro";
import CardTestimonials from "./widgets/Card-Testimonials.astro";


---

<section class="bg-center font-mont py-20 bg-orange-100">
    <div class="px-4 mx-auto max-w-screen-xl">
        <h1 class="pt-20 text-4xl font-extrabold tracking-tight leading-none text-gunmetal md:text-xl lg:text-4xl uppercase text-center">Testimonials
        </h1>
        <img src="assets/pata2-cinnabar.png" alt="decor" class="mx-auto">
          <!-- <div class="flex flex-col space-y-4 sm:flex-row sm:justify-center sm:space-y-0">
              <div class="grid grid-cols-3 gap-5 justify-between items-start pt-10">
                  {testimonails.map(testimonial => (
                      <CardTestimonials content={testimonial.content} name={testimonial.name} role={testimonial.role} />
                      )
                    ) 
                  }      
              </div>
            </div> -->

            <CardTestimonialL/>
            
        </div>
    </div>
</section>